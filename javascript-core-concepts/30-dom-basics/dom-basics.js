// DOM (Document Object Model) allows JS to interact with HTML

// Selecting an element
const heading = document.querySelector("h1");

// Changing text
heading.textContent = "Hello, Avijit!";

// Changing style
heading.style.color = "blue";

console.log(heading);
