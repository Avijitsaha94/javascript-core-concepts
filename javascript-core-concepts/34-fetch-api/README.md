# Fetch API

## What is Fetch API?
Fetch API provides a modern way to make HTTP requests in JavaScript.

## How it works
- fetch(url) returns a Promise
- Use .then() to handle the response
- Use .catch() to handle errors

## Why use Fetch API
- Replace old XMLHttpRequest (AJAX)
- Works well with Promises / Async Await
