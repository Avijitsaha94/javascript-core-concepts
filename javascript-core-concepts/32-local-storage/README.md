# Local Storage

## What is Local Storage?
Local Storage is a browser storage that keeps data
even after closing or reloading the page.

## Common Methods
- setItem(key, value)
- getItem(key)
- removeItem(key)
- clear()

## Why use Local Storage
- Save user preferences
- Store temporary data
- Avoid repeated server calls
